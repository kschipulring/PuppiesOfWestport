<?xml version="1.0" encoding="UTF-8"?>
<config>
    <modules>
        <ThreeRingDesign_SkuSorter>

            <!-- Whether our module is active: true or false -->
            <active>true</active>

            <!-- Which code pool to use: core, community or local -->
            <codePool>local</codePool>

            <depends>
                <Mage_Catalog/>
            </depends>
        </ThreeRingDesign_SkuSorter>
    </modules>
</config>